package tunnel

import (
	"testing"
)

func TestProtocol(t *testing.T) {
	// frame := &ControlFrame{
	// 	Type: FramePing,
	// }

	// // err := WriteFrame(stream, frame) // stream 是一个 net.Conn 或 yamux.Stream
	// // if err != nil {
	// // 	panic(err)
	// // }

	// // recv, err := ReadFrame(stream)
	// // if err != nil {
	// // 	panic(err)
	// // }

	// // if recv.Type == FramePong {
	// // 	fmt.Println("Pong received")
	// // }
}
